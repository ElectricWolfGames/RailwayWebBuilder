<UserControl x:Class="LocoDBSync.LocoDetails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="64" d:DesignWidth="1000">
    <Grid>
        <TextBox Text="{Binding CatalogueId, Mode=OneWay}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="55" />
        <TextBox Text="{Binding RunningNumber,Mode=OneWay}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="57" Margin="60,0,0,0" />
        <TextBox Text="{Binding LocoName,Mode=OneWay}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="522" Margin="122,0,0,0" />
    </Grid>
</UserControl>